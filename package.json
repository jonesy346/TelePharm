{
  "name": "ntw",
  "version": "0.0.0",
  "scripts": {
    "test": "tap --no-watch test.ts",
    "watch": "tap test.ts"
  },
  "devDependencies": {
    "@types/tap": "^15.0.5",
    "tap": "^15.0.10",
    "ts-node": "^10.4.0",
    "typescript": "^4.4.4"
  },
  "tap": {
    "reporter": "spec",
    "watch": true,
    "node-arg": [
      "--require=ts-node/register"
    ]
  }
}
